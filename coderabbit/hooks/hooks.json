{
  "description": "CodeRabbit PR gate: blocks `gh pr create|edit|ready|merge|reopen` until /coderabbit has run clean on the current HEAD (approval receipt), and hard-blocks on secrets found by gitleaks regardless of receipt.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/skills/coderabbit/scripts/pr-gate.sh\""
          }
        ]
      }
    ]
  }
}
