{
  "description": "CodeHare PR gate: blocks `gh pr create|edit|ready|merge|reopen` until /codehare has run clean on the current HEAD (approval receipt), and hard-blocks on secrets found by gitleaks regardless of receipt.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/skills/codehare/scripts/pr-gate.sh\""
          }
        ]
      }
    ]
  }
}
